{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "allowJs": false,
    "checkJs": false,
    "types": ["node", "jest", "supertest", "Multer"],
    "rootDir": ".."
  },
  "exclude": ["../node_modules", "../dist"],
  "include": ["../src/**/*.ts", "./**/*.ts"]
}
