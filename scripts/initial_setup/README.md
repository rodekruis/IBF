# Initial setup

Run the `initial_setup.sh` script to create a completely new IBF environment:

```shell
initial_setup.sh {TARGET_DIRECTORY}
```

This script is made for macOS computers.

The `sub_scripts` folder contains scripts that should be executed from the directory containing the `IBF` and `go-web-app` directories. So don't run them from here.
