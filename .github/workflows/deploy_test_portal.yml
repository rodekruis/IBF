# See: https://docs.github.com/en/actions/reference/workflow-syntax-for-github-actions

name: 'Deploy [test] Portal'

env:
  workingDirectory: portal/nrw-standalone

permissions:
  contents: read
  actions: read
  deployments: write
  id-token: write
  # Disabling this to avoid spamming the PR with "Your stage site is ready!" comments
  # https://github.com/Azure/static-web-apps/issues/1135#issuecomment-1750066331
  # pull-requests: write

on:
  workflow_call:
    secrets:
      AZURE_CLIENT_ID:
        required: true
      AZURE_TENANT_ID:
        required: true
      AZURE_SUBSCRIPTION_ID:
        required: true
      APP_MAPBOX_ACCESS_TOKEN:
        required: true
      FONTAWESOME_NPM_AUTH_TOKEN:
        required: true
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/deploy_test_portal.yml'
      - '.github/actions/build-frontend/**'
      - 'portal/**'
      - '!*.md'
      - '!*.yml'
  pull_request:
    types: [opened, synchronize, reopened, closed]
    paths:
      - '.github/workflows/deploy_test_portal.yml'
      - '.github/actions/build-frontend/**'
      - 'portal/**'
      - '!*.md'
      - '!*.yml'

jobs:
  test:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      deployments: write
      id-token: write
      # Disabling this to avoid spamming the PR with "Your stage site is ready!" comments
      # https://github.com/Azure/static-web-apps/issues/1135#issuecomment-1750066331
      # pull-requests: write
    environment: 'test'
    env:
      APP_IBF_API_BACKEND: ${{ vars.APP_NRW_API_BACKEND }}
      APP_SEED_DATA_REPO: ${{ vars.APP_SEED_DATA_REPO }}
      APP_MAPBOX_ACCESS_TOKEN: ${{ secrets.APP_MAPBOX_ACCESS_TOKEN }}
      FONTAWESOME_NPM_AUTH_TOKEN: ${{ secrets.FONTAWESOME_NPM_AUTH_TOKEN }}
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
    outputs:
      preview_url: ${{ steps.deploy_to_aswa.outputs.static_web_app_url }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Build Frontend
        id: build
        uses: ./.github/actions/build-frontend
        with:
          frontendPath: ${{ env.workingDirectory }}

      - name: Get API key for Azure Static Web App
        id: get-api-key
        uses: ./.github/actions/get-swa-api-key
        with:
          azureClientId: ${{ env.AZURE_CLIENT_ID }}
          azureTenantId: ${{ env.AZURE_TENANT_ID }}
          azureSubscriptionId: ${{ env.AZURE_SUBSCRIPTION_ID }}
          staticWebAppName: nrw-portal-test

      # More information on Static Web App workflow configurations,
      # See: https://aka.ms/swaworkflowconfig
      - name: Deploy to Azure Static Web App
        id: deploy_to_aswa
        uses: Azure/static-web-apps-deploy@v1
        env:
          SKIP_DEPLOY_ON_MISSING_SECRETS: true
        with:
          azure_static_web_apps_api_token: ${{ steps.get-api-key.outputs.apiKey }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          config_file_location: '${{ env.workingDirectory }}/dist'
          app_location: '${{ env.workingDirectory }}/dist'
          output_location: ''
          skip_app_build: true
          skip_api_build: true

      - name: Finish
        run: |
          echo "Build version: ${{ steps.build.outputs.build_version }}" >> $GITHUB_STEP_SUMMARY
          echo "Preview: <${{ steps.deploy_to_aswa.outputs.static_web_app_url }}>" >> $GITHUB_STEP_SUMMARY

  add_preview_link:
    if: (github.event_name == 'pull_request' && github.event.action != 'closed' && needs.test.outputs.preview_url != '')
    needs: test
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Add Deployment URL to PR Description
        uses: nefrob/pr-description@v1.3.0
        with:
          content: ${{ needs.test.outputs.preview_url }}
          regex: '<!-- start deployment url -->.*?<!-- end deployment url -->'
          regexFlags: ims
          appendContentOnMatchOnly: true
          token: ${{ secrets.GITHUB_TOKEN }}

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    environment: 'test'
    env:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Get API key for Azure Static Web App
        id: get-api-key
        uses: ./.github/actions/get-swa-api-key
        with:
          azureClientId: ${{ env.AZURE_CLIENT_ID }}
          azureTenantId: ${{ env.AZURE_TENANT_ID }}
          azureSubscriptionId: ${{ env.AZURE_SUBSCRIPTION_ID }}
          staticWebAppName: nrw-portal-test

      - name: 'Close Pull Request: Deploy to Azure Static Web App'
        uses: Azure/static-web-apps-deploy@v1
        env:
          SKIP_DEPLOY_ON_MISSING_SECRETS: true
        with:
          app_location: '${{ env.workingDirectory }}/dist'
          azure_static_web_apps_api_token: ${{ steps.get-api-key.outputs.apiKey }}
          action: 'close'
