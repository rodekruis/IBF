
class ConfigReader:
    """
    This class reads the configuration file and provides access to its contents.
    """

    def try_read_config(self, config_path, config_target) -> bool:
        """
        Try to load the config file. Return False on failure.
        """
        return True
    
    def parse_all(self, path) -> bool:
        """
        Parse all config files to make sure there are no formatting errors.
        """
        return True