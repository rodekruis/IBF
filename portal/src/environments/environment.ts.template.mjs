import { ALL_AVAILABLE_LOCALES } from '../../_all_available-locales.mjs';

// Output should be a valid TS-file:
export default `// THIS FILE IS GENERATED BY 'npm run set-env-variables'

export const environment = {
  production: ${process.env.NG_PRODUCTION || 'true'},

  // Configuration/Feature-switches:
  defaultLocale: '${process.env.NG_DEFAULT_LOCALE || 'en-GB'}',
  locales: '${process.env.NG_LOCALES || ALL_AVAILABLE_LOCALES}',
  envName: '${process.env.NG_ENV_NAME || ''}',

  // APIs:
  url_api_service: '${process.env.NG_URL_API_SERVICE || ''}',

  // Monitoring/Telemetry:
  applicationinsights_connection_string: '${process.env.APPLICATIONINSIGHTS_CONNECTION_STRING || ''}',
  matomo_connection_string: '${process.env.MATOMO_CONNECTION_STRING || ''}',
};
`;
